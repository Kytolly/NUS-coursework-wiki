name: Deploy Wiki to GitHub Pages

# Builds the MkDocs site from the canonical Markdown in page/ and publishes it to
# GitHub Pages. The generated tree (build/) is never committed: script/build.py
# projects page/ into build/preview/docs, then MkDocs renders build/preview/site.
#
# Pages is already enabled for this repository with build_type=workflow, so the
# workflow only needs to read Pages metadata and deploy the artifact. Enabling
# Pages from a workflow requires a token other than GITHUB_TOKEN, so it is done
# once out-of-band (Settings -> Pages -> Source: GitHub Actions) instead.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment, but never cancel an in-progress one.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: script/requirements.txt

      - name: Install MkDocs toolchain
        run: |
          python -m pip install --upgrade pip
          pip install -r script/requirements.txt

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Project page/ into the MkDocs docs tree
        run: python script/build.py

      - name: Build the site
        run: mkdocs build -f script/mkdocs.yml

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: build/preview/site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
